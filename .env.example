# Poller (GitHub Actions / local). NUNCA commitar o .env real.
SUPABASE_URL=https://auyncgppjawgsbxfrmpk.supabase.co
SUPABASE_SERVICE_KEY=sb_secret_xxx            # service/secret key (server-side, bypassa RLS)
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/xxx/yyy
DASHBOARD_URL=https://seu-painel.vercel.app   # opcional, entra no alerta

# Frontend (web/config.js) usa a PUBLISHABLE key (publica), nao estas.
